'use client';

import { Button } from '@heroui/react';

export default function EnrollmentButton() {
   return (
      <Button color="primary" size="lg" className="w-full font-bold shadow-lg mt-4">
         Enroll Now
      </Button>
   );
}
